<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Excursiones Joserak - Viajes Inolvidables</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1920&q=80') no-repeat center center fixed;
      background-size: cover;
      color: #fff;
      scroll-behavior: smooth;
    }

    .overlay {
      background: rgba(0, 0, 0, 0.7);
      min-height: 100vh;
      padding-bottom: 40px;
    }

    header {
      text-align: center;
      padding: 60px 20px;
    }

    header h1 {
      font-size: 2.5rem;
      color: #00d9ff;
      font-weight: 700;
      text-shadow: 0 0 15px #00d9ff;
    }

    header h2 {
      font-size: 1.2rem;
      color: #f0f0f0;
      margin-top: 10px;
    }

    /* ===== GRID DE SECCIONES ===== */
    .grid-container {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 25px;
      padding: 40px;
      max-width: 1400px;
      margin: auto;
    }

    section {
      background: rgba(255, 255, 255, 0.08);
      padding: 25px;
      border-radius: 15px;
      backdrop-filter: blur(10px);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
    }

    section:hover {
      transform: translateY(-5px);
      box-shadow: 0 0 15px rgba(0, 224, 255, 0.4);
    }

    h3 {
      color: #00e0ff;
      border-bottom: 2px solid #00e0ff;
      display: inline-block;
      margin-bottom: 15px;
    }

    ul {
      list-style: none;
      padding-left: 0;
    }

    ul li {
      margin: 8px 0;
    }

    .icon {
      color: #00e0ff;
      margin-right: 10px;
    }

    table {
      font-size: 0.9rem;
    }

    footer {
      text-align: center;
      margin-top: 30px;
      padding: 20px;
      color: #ccc;
      font-size: 0.9rem;
    }

    /* ===== CALENDARIO ===== */
    #calendario {
      text-align: center;
    }

    #calendar-container {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: 5px;
      margin-top: 15px;
    }

    .day-name {
      font-weight: 600;
      color: #00e0ff;
      background: rgba(255, 255, 255, 0.05);
      padding: 10px 0;
      border-radius: 8px;
    }

    .day {
      background: rgba(255, 255, 255, 0.1);
      padding: 10px 0;
      border-radius: 8px;
      text-align: center;
      cursor: pointer;
      transition: 0.3s;
    }

    .day:hover {
      background: rgba(0,224,255,0.3);
    }

    .event-day {
      background: rgba(0,224,255,0.6);
      color: #000;
      font-weight: bold;
      border: 2px solid #00ffcc;
      box-shadow: 0 0 10px rgba(0,224,255,0.8);
    }

    .selected {
      background: #00ff88 !important;
      color: #000 !important;
    }

    #month-selector {
      background: rgba(0, 0, 0, 0.3);
      color: #00e0ff;
      border: 1px solid #00e0ff;
      border-radius: 10px;
      padding: 6px 10px;
      margin-top: 10px;
      font-weight: 600;
    }

    /* Navbar */
    .navbar {
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(10px);
    }

    .navbar a {
      color: #fff;
      font-weight: 500;
      text-decoration: none;
      margin: 0 10px;
    }

    .navbar a:hover {
      color: #00e0ff;
    }

    /* ===== IMAGEN FINAL ===== */
    .imagen-final {
      width: 100%;
      margin-top: 40px;
      text-align: center;
      position: relative;
    }

    .imagen-final img {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      border-radius: 20px;
      box-shadow: 0 0 25px rgba(0, 224, 255, 0.4);
      transition: 0.6s ease;
      opacity: 0.9;
    }

    .imagen-final img:hover {
      transform: scale(1.02);
      opacity: 1;
    }

    .imagen-final h4 {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      background: rgba(0,0,0,0.6);
      padding: 10px 25px;
      border-radius: 15px;
      font-size: 1.3rem;
      color: #00e0ff;
      font-weight: 600;
      text-shadow: 0 0 10px #00e0ff;
    }

    /* RESPONSIVE */
    @media (max-width: 1200px) {
      .grid-container {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 768px) {
      .grid-container {
        grid-template-columns: 1fr;
        padding: 20px;
      }

      header h1 { font-size: 2rem; }
    }

    /* REDES SOCIALES FIJAS */
    .social-bottom {
      position: fixed;
      bottom: 20px;
      right: 20px;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 1000;
    }

    .social-bottom a {
      color: white;
      font-size: 1.6rem;
      width: 50px;
      height: 50px;
      display: flex;
      justify-content: center;
      align-items: center;
      border-radius: 50%;
      text-align: center;
      transition: 0.3s;
    }

    .social-bottom a:nth-child(1) { background: #3b5998; }
    .social-bottom a:nth-child(2) { background: #C13584; }
    .social-bottom a:nth-child(3) { background: #25D366; }

    .social-bottom a:hover { transform: scale(1.1); opacity: 0.9; }
  </style>
</head>
<body>
  <div class="overlay">

    <!-- Navbar -->
    <nav class="navbar fixed-top">
      <div class="container d-flex justify-content-center flex-wrap">
        <a href="#puerto">Puerto</a>
        <a href="#ixtapa">Ixtapa</a>
        <a href="#acapulco">Acapulco</a>
        <a href="#calendario">Calendario</a>
      </div>
    </nav>

    <!-- HEADER -->
    <header>
      <h1>🌴 Excursiones Joserak 🌊</h1>
      <h2>¡Viajar con nosotros es una experiencia inolvidable!</h2>
    </header>

    <!-- GRID PRINCIPAL DE SECCIONES -->
    <div class="grid-container">

      <!-- Puerto -->
      <section id="puerto">
        <h3><i class="fa-solid fa-location-dot icon"></i>Puerto Escondido</h3>
        <p><strong>Fechas:</strong> 5 al 8 de Noviembre 2025</p>
        <table class="table table-dark table-striped mt-2">
          <tr><th>Habitación</th><th>Personas</th><th>Costo</th></tr>
          <tr><td>Compartida</td><td>5-6</td><td>$3,800</td></tr>
          <tr><td>Intermedia</td><td>3-4</td><td>$4,100</td></tr>
          <tr><td>Doble</td><td>2</td><td>$4,400</td></tr>
        </table>
        <ul>
          <li>✅ Autobús de lujo</li>
          <li>✅ 3 noches de hospedaje</li>
          <li>💳 Aparta con $2,000</li>
        </ul>
      </section>

      <!-- Ixtapa -->
      <section id="ixtapa">
        <h3><i class="fa-solid fa-umbrella-beach icon"></i>Ixtapa Zihuatanejo</h3>
        <p><strong>Fechas:</strong> 18 al 21 de Diciembre 2025</p>
        <table class="table table-dark table-striped mt-2">
          <tr><th>Habitación</th><th>Personas</th><th>Costo</th></tr>
          <tr><td>Compartida</td><td>5-6</td><td>$3,400</td></tr>
          <tr><td>Intermedia</td><td>3-4</td><td>$3,600</td></tr>
          <tr><td>Doble</td><td>2</td><td>$3,800</td></tr>
        </table>
        <ul>
          <li>✅ Transporte en autobús</li>
          <li>✅ Hospedaje 3 noches</li>
          <li>💳 Aparta con $2,000</li>
        </ul>
      </section>

      <!-- Acapulco -->
      <section id="acapulco">
        <h3><i class="fa-solid fa-fireworks icon"></i>Acapulco Fin de Año 🎆</h3>
        <p><strong>Viaje Express:</strong> 31 diciembre 2025</p>
        <p>🎇 Disfruta la pirotecnia en la bahía.</p>
        <ul>
          <li>🚌 Salida 2:00 AM</li>
          <li>🏖️ Regreso 1:30 AM (1 de enero)</li>
          <li>🔥 Ideal para despedir el año con estilo</li>
        </ul>
      </section>

      <!-- Calendario -->
      <section id="calendario">
        <h3><i class="fa-solid fa-calendar-days icon"></i>Calendario</h3>
        <p>Selecciona el mes y marca tu excursión 🌴</p>

        <select id="month-selector">
          <option value="2025-11">🗓️ Noviembre 2025</option>
          <option value="2025-12">🎄 Diciembre 2025</option>
        </select>

        <div id="calendar-container"></div>
        <p id="selected-event" class="mt-3"></p>
      </section>

    </div>

    <!-- IMAGEN FINAL -->
    <div class="imagen-final">
      <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=1500&q=80" alt="Viajes Joserak">
      <h4>Descubre, Disfruta y Vive la Aventura 🌴</h4>
    </div>

    <footer>
      © 2025 Excursiones Joserak | 🌴 Diseño web profesional
    </footer>

    <!-- Redes sociales -->
    <div class="social-bottom">
      <a href="https://www.facebook.com/share/1JSDQ6bVUq/" target="_blank"><i class="fab fa-facebook-f"></i></a>
      <a href="https://www.instagram.com/" target="_blank"><i class="fab fa-instagram"></i></a>
      <a href="https://wa.me/527771038952" target="_blank"><i class="fab fa-whatsapp"></i></a>
    </div>

    <!-- SCRIPT CALENDARIO -->
    <script>
    const calendarContainer = document.getElementById('calendar-container');
    const monthSelector = document.getElementById('month-selector');
    const selectedEvent = document.getElementById('selected-event');

    const eventos = {
      '2025-11-05': 'Puerto Escondido 🌊',
      '2025-11-06': 'Puerto Escondido 🌊',
      '2025-11-07': 'Puerto Escondido 🌊',
      '2025-11-08': 'Puerto Escondido 🌊',
      '2025-12-18': 'Ixtapa 🌴',
      '2025-12-19': 'Ixtapa 🌴',
      '2025-12-20': 'Ixtapa 🌴',
      '2025-12-21': 'Ixtapa 🌴',
      '2025-12-31': 'Acapulco 🎆'
    };

    const daysOfWeek = ['Dom', 'Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb'];

    function generarCalendario(year, month) {
      calendarContainer.innerHTML = '';
      selectedEvent.innerText = '';
      const firstDay = new Date(year, month - 1, 1).getDay();
      const totalDays = new Date(year, month, 0).getDate();

      daysOfWeek.forEach(d => {
        const div = document.createElement('div');
        div.className = 'day-name';
        div.textContent = d;
        calendarContainer.appendChild(div);
      });

      for (let i = 0; i < firstDay; i++) {
        calendarContainer.appendChild(document.createElement('div'));
      }

      for (let d = 1; d <= totalDays; d++) {
        const dateKey = `${year}-${String(month).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
        const div = document.createElement('div');
        div.className = 'day';
        div.textContent = d;

        if (eventos[dateKey]) {
          div.classList.add('event-day');
          div.title = eventos[dateKey];
        }

        div.onclick = () => {
          div.classList.toggle('selected');
          mostrarEventosSeleccionados();
        };

        calendarContainer.appendChild(div);
      }
    }

    function mostrarEventosSeleccionados() {
      const seleccionados = Array.from(document.querySelectorAll('.day.selected'))
        .map(d => d.title)
        .filter(Boolean);
      selectedEvent.textContent = seleccionados.length > 0 ?
        `Seleccionaste: ${[...new Set(seleccionados)].join(', ')}` : '';
    }

    generarCalendario(2025, 11);
    monthSelector.onchange = e => {
      const [year, month] = e.target.value.split('-');
      generarCalendario(+year, +month);
    };
    </script>

  </div>
</body>
</html>
